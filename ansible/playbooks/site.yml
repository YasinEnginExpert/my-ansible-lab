#=============================================#
# Main Orchestration Playbook
# Calls all playbooks in the correct sequence
# Use tags to run specific workflows
#=============================================#
---
- import_playbook: interfaces.yml
  tags:
    - interfaces

- import_playbook: routing.yml
  tags:
    - routing

- import_playbook: verify.yml
  tags:
    - verify